# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BDFComponent' do
    use_frameworks!
    pod 'MBProgressHUD', '~> 1.1.0', :modular_headers => true
end
